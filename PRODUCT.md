# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

Primary user: فارسی‌زبان علاقه‌مند به لایف‌استایل (فیلم، کتاب، کافه، سفر و مشابه) که می‌خواهد سریع لیست کیوریتد پیدا کند، ذخیره کند، و بعداً لیست شخصی بسازد.

Situations:
- مهمان: کشف و ذخیرهٔ سبک؛ ورود برای همگام‌سازی
- کاربر فعال: ذخیره، ساخت لیست، پروفایل، همکاری روی لیست شخصی
- ادمین/کیوریتور داخلی: مدیریت کاتالوگ و محتوا (پنل جدا)

## Product Purpose

Wibe (وایپ/وایپ‌کور) یک PWA موبایل‌اول برای **کشف و اشتراک لیست‌های کیوریتد لایف‌استایل** است. موفقیت یعنی کاربر در چند ثانیه یک لیست مرتبط پیدا کند، ذخیره کند، و مسیر «برای تو» / ساخت لیست برایش روشن باشد.

## Positioning

کشف mood-first («امروز دنبال چه وایبی هستی؟») به‌همراه کاتالوگ لیست‌ها و ذخیرهٔ اول — نه فید شبکه‌اجتماعی عمومی و نه مارکت‌پلیس.

## Capabilities

- خانه: اسپات‌لایت منتخب، فید، onboarding علاقه‌مندی
- لیست‌ها (`/lists`): کاتالوگ browse با mode ترند/جدید/محبوب/ذخیره و فیلتر
- اکسپلور (`/explore`): کشف بر اساس حال‌وهوا، ترند، دسته؛ مسیر رسمی کشف
- آیتم‌ها، جستجو، پروفایل، لیست شخصی (`/user-lists/[id]`)
- پنل ادمین دسکتاپ برای محتوا و عملیات

## Constraints

- RTL + فارسی (Vazirmatn)
- Mobile-first؛ شل دسکتاپ ~1200px
- Consumer **light-only** (نه dark toggle): `color-scheme: light` اجباری؛ کلاس `.dark` روی `<html>` ست نمی‌شود
- مسیر قدیمی `/user-lists` (لندینگ) به `/explore` redirect می‌شود؛ جزئیات لیست شخصی روی `/user-lists/[id]` می‌ماند

## Terminology

| Term | Meaning |
|------|---------|
| وایب / mood | حال‌وهوا یا موقعیت کشف (مثلاً قبل خواب، کافه) |
| لیست | مجموعه کیوریتد یا کاربرساخته از آیتم‌ها |
| اکسپلور | سطح کشف mood-first (`/explore`) |
| لیست‌ها | کاتالوگ browse (`/lists`) |
| ذخیره / bookmark | ذخیره لیست برای بعد و شخصی‌سازی |
| منتخب هفته | اسپات‌لایت فیچرد روی خانه |

## Accessibility

- Skip link به محتوای اصلی
- زوم صفحه مجاز است (بدون `maximumScale` قفل)
- کنترل‌های آیکونی باید `aria-label` داشته باشند
- `prefers-reduced-motion` در globals رعایت می‌شود

## Open decisions

- نام برند در UI گاهی «وایپ» / SITE_NAME از تنظیمات — منبع واحد copy برای مارکتینگ باز است

## Confirmed product decisions

- Consumer **light-only** تا اطلاع بعدی: `color-scheme: light`؛ کلاس `.dark` و توکن‌های dark فقط رزرو برای آینده
- مسیر رسمی اکسپلور: `/explore` (لندینگ `/user-lists` فقط redirect)

## Progress log

کارهای انجام‌شدهٔ consumer UX (تا تکرار نشود): [`docs/CONSUMER_UX_PROGRESS.md`](./docs/CONSUMER_UX_PROGRESS.md)
