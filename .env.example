# ===========================================
# وایب‌کر – نمونه متغیرهای محیطی
# این فایل را کپی کن به .env و مقادیر را پر کن
# ===========================================

# ---- دیتابیس (الزامی) ----
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/wibecur"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/wibecur"

# ---- احراز هویت (الزامی) ----
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="https://your-domain.com"

# ---- رمزنگاری (الزامی) ----
# کلید ۳۲ کاراکتری برای رمز API keys در دیتابیس
ENCRYPTION_KEY="your-32-char-encryption-key"

# ---- آدرس سایت (توصیه‌شده) ----
# برای SEO، تصاویر OG، sitemap
NEXT_PUBLIC_APP_URL="https://your-domain.com"

# ---- Sentry (توصیه‌شده) ----
# NEXT_PUBLIC_SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"
# SENTRY_ORG="your-org"
# SENTRY_PROJECT="your-project"
# SENTRY_AUTH_TOKEN="sntrys_xxx"   # فقط در CI/CD برای source map

# ---- Object Storage – Liara ----
# برای اسکریپت set-liara-config و ذخیره در دیتابیس
# LIARA_BUCKET_NAME=your-bucket
# LIARA_ENDPOINT=https://storage.c2.liara.space
# LIARA_ACCESS_KEY=
# LIARA_SECRET_KEY=

# ---- Rate Limiting (Upstash Redis) ----
# برای محدود کردن درخواست‌های API. بدون این متغیرها rate limiting غیرفعال است.
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxx

# ---- Cron ----
# CRON_SECRET=secret-for-cron-endpoints

# ---- اختیاری ----
# NEXT_PUBLIC_SKIP_EXTERNAL_IMAGES=true   # غیرفعال کردن تصاویر خارجی
# PORT=3000
